= turbo_stream.update dom_id(@virtual_machine) do
  = render "form"

= turbo_stream.update "connection_info" do
  = render "virtual_machines/connection_info", vm: @virtual_machine

= turbo_stream.update dom_id(@virtual_machine, :address_preview) do
  = render 'virtual_machines/address_preview'